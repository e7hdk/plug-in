# <Capability> — capability profile

<!-- A capability is a cross-cutting feature axis (mobile, ai/ml, i18n, payments, agency) that a
host turns ON regardless of depth. /init-org / /scale-org weave this into AGENTS.md and add the
capability's agents to the active roster. -->

## What this capability adds
<the new concerns/constraints it introduces across the product>.

## Agents it activates
<agent names this capability adds to the roster>.

## Gates / concerns
<the non-negotiables this capability introduces — compliance, perf budgets, rollback rules, …>.

## Role notes
- builders/qa: <what changes for them>.
- security/release: <capability-specific risks and process>.
